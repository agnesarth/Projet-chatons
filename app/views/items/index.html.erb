
<div class="jumbotron">
  <h1 class="display-3">Chatons lovers</h1>
  <p class="lead">Un espace pour les amoureux des félins.</p>
  <hr class="my-4">
  <p>Oui, on le sait, les chats c’est trop mignon ! Des images de chatons d'une qualité jamais égalée. </p>
  <p class="lead">
    <a class="btn btn-primary btn-lg" href="users/sign_in" role="button">Trouver un chaton</a>
  </p>
</div>

<div class="card-columns border-info container">
  <% @items.each do | item | %>
    <div class="card border-secondary mb-4">
      <img class="card-img-top" src=<%= item.image_url %> alt="Cute cat image">
      <div class="card-body">
        <p class="card-text "><b> <%= sprintf('%.2f', item.price) %> €</b></p>
          <div class="d-flex justify-content-between">
            <%= link_to "Voir le produit", item_path(item), class: "btn btn-outline-primary btn-sm mr-3"%>
            <%= link_to "javascript:void(0)", class: "btn btn-primary btn-sm ml-auto" do %><i class="fa fa-heart"></i><% end %>
            <%= link_to items_path(id: item.id), method: :post, class: "btn btn-primary btn-sm ml-1" do %><i class="fa fa-shopping-cart"></i><% end %>
          </div>
      </div>
    </div>
  <% end %>
</div>
 